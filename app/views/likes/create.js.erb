$("#like-buttons").html("<%= j(render partial: 'news/like', locals: {pick: @pick, picks: @picks, like: @like, likes: @likes}) %>");

// $("#follow-count").html("<%= escape_javascript(render('users/follow_count')) %>");
